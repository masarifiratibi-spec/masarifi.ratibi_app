# Masarifi

Masarifi is organized as a future multi-application workspace. The approved Admin Dashboard is the only implemented application.

Run it with:

```powershell
cd apps/admin-web
npm install
npm run dev
```

See `PROJECT_STRUCTURE.md` for the repository map and intentionally unimplemented areas.
