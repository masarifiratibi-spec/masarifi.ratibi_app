# Android Evidence Status - 2026-08-09

Status: EXECUTED

Device: RK8XB00N33K, SM-A165F, Android 16, arm64-v8a
Package: com.masarifi.mobile
Build path: Expo development client on Metro http://localhost:8084

Evidence:
- Launch/auth/home: `devclient-open.*`, `after-google-account.xml`
- Tracking status: `tracking-home.*`, `after-restart-tracking.*`
- SMS permission: `permission-revoked.txt`, `permission-restored.txt`, `app-settings.*`
- Pause/resume: `paused.*`, `actions-bottom.*`, `after-resume.*`
- Demo and undo: `demo.*`, `demo-processed.*`, `undo.*`
- Restart/background: `devclient-restart.*`, `after-restart-tracking.*`
- Theme: `light.*`, `dark.*`
- 200% text: `font-200.*`
- Device size: `small-screen.*`
- Accessibility/TalkBack surface: `accessibility-services.txt` plus UIAutomator XML content descriptions in captured screens
- Battery/system state: `battery-whitelist.txt`
- Runtime logs: `logcat-last-errors.txt`

Notes:
- Screenshots were captured for every state. UIAutomator XML is the primary readable evidence because the dev-client/performance overlay can make some screenshots appear dark.
- Offline and battery-restricted recovery are implemented as mocked service states in the JavaScript build; no native Android service toggle exists in this dev build to force those states from ADB.
- Direct `masarifi://tracking` opens the Expo dev launcher selector in this dev build. The verified native launch path is the Expo dev-client URL.

Functional follow-up:
- Pause/resume, clear-event auto-add, Undo, uncertain-event review, localized review detail, and confirmation were exercised on the physical device.
- Revoking `READ_SMS` through Android now changes status to `Turned off in settings` and exposes `Open settings` plus manual fallback.
- Restoring `READ_SMS` changes status back to `Granted` when the screen is reopened; the status query also refreshes after returning from Settings.
- Production inbox reading and a notification listener remain intentionally absent; only the approved mock-event flow was validated.
- Follow-up evidence: `full-review-2026-08-09/13-tracking-revoked-native.xml` and `14-tracking-permission-restored.xml`.
